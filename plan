/*
		    1) загрузить конфиг
		    2) распечатать инфу о программе
		
        	3) получить текст запроса

			4) проверить запрос на соответствие SELECT-FROM-WHERE
				a) если не соответствует - ошибка и выход

			2) SplitQuery
				a) вытащить секцию SELECT
				б) вытащить секцию FROM   **посмотреть, как обрабатываются имена файлов.
					- проверить, все ли указанные файлы существуют - если нет - то выход
				в) вытащить секцию WHERE
					- получить rpn

			3) запустить ctx с таймаутом
			4) запустить слушателя сигнала SIGINT, дать ему ctx.cancel()

		5) цикл
			ф) - закрыт ли контекст?
			ы) открыть файл
			а) проверить, все ли столбцы из запроса имеются в файле
			б) если это первый файл - вывести шапку (поля, указанные в SELECT)
				если не первый - то ничего с ней не делать
			в) go (rpn, ctx)
					- закрыт ли контекст ?
				  i) прочитать строку csv
				 ii) получить valuesMap
				iii) rpn.InsertValues
				 iv) если rpn.CalculateRPN == true, то print полей из SELECT иначе - игнор
				 v) конец файла? если да, то финиш го-рутины, если нет, то читаем дальше
			г) закрыть открытый файл

		6)




** посмотреть, как обрабатываются имена файлов в секции FROM (без кавычек, с расширением...)
** ... то же самое про запятые 
	*/